$('#item-<%=@cart_item[:id]%>').html("<%= j (render partial: 'cart_item',  :locals => {:item => @cart_item}) %>");
$('#total_area').html("<%= j (render partial: 'total_area',:locals => {:tax => @tax, :cart_sub_total => @cart_sub_total}) %>");
